# Backend API requirements
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
python-multipart>=0.0.6

# Optional: For production
gunicorn>=21.2.0

# Optional: For caching (Redis)
# redis>=5.0.0
# hiredis>=2.2.3

# Note: Main dependencies (sentence-transformers, faiss-cpu, etc.)
# should already be installed from ../requirements.txt
